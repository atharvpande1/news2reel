# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local runtime state — see CLAUDE.md: the DB is never committed or shared
# via files; it's created by `alembic upgrade head`.
*.db
*.db-wal
*.db-shm
.env

